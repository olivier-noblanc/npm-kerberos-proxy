{
  "name": "npm-kerberos-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Agent proxy Kerberos/Negotiate pour npm sur Windows, avec installeur autonome",
  "scripts": {
    "pretest": "node -e \"require('fs').cpSync('test/fixtures/kerberos-stub', 'node_modules/kerberos', {recursive: true})\"",
    "test": "node --experimental-test-module-mocks --test test/**/*.test.js"
  },
  "license": "MIT",
  "engines": {
    "node": ">=20"
  }
}
